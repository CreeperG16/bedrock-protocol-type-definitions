import { zigzag32 } from "../types";

export type set_display_objective = {
  display_slot: string;
  objective_name: string;
  display_name: string;
  criteria_name: string;
  sort_order: zigzag32;
};
